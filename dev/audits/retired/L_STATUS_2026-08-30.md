# UNIFIED_AUDIT §1: current true status of L-001 to L-022
**Updated:** 2026-08-30 · **Version:** v3.14.256  
*Frozen at the version it was taken. Not bumped again.*

> **RETIRED at v3.14.390.** `AUDIT_INDEX.md` ruled it retirable at v3.14.310 and
> the move is the execution, not a new judgement. **What replaced it:**
> `UNIFIED_AUDIT.md` bumps with every version and computes its own counts from
> its own tables, so a second document reconciling L-001 to L-022 is the
> two-writers shape (PRACTICES §4) — and this one has been the wrong writer since
> v3.14.256. Six of its nine "still open" have shipped; it predates L-023 and
> L-024 entirely, and the audit now runs to **L-042**.
>
> **Also superseded by the data.** Both corpora it measured are gone: the live
> corpus it names at `~/mnt/turkish_test_corpus` and the fixture at
> `samples/turkish-test` were both replaced by the fixture swap at v3.14.384, so
> every byte count and every coverage figure below is about files that no longer
> exist. See `dev/design/D58_fixture_set.md` §§7-10.
>
> **Still worth reading:** the *Method* paragraph. Mutating ten top-level
> functions with `return null;` and re-running the whole suite is the technique
> that `GUARD_MUTATION_2026-08-30.md` then made routine, and it is stated more
> compactly here than anywhere else.


**Audited against:** v3.14.254, `source/version.py`. Repository at
`~/mnt/Corpus Builder`. Live corpus at `~/mnt/turkish_test_corpus`
(`turkish_test_corpus_corpus.jsonl` 78,282 bytes, `_dictionary.jsonl` 26,709,
last written 2026-08-30 01:54). Fixture at `samples/turkish-test`.

**Method.** Every claim was executed rather than read where the question allowed
it: the guard suite was run against `samples/` and against the live corpus via
`LINGCOT_TEST_CORPUS`; ten top-level functions were mutated by prepending
`return null;` in a scratch copy of the tree and the whole suite re-run against
each; the corpus and dictionary were loaded through the app's own loaders in a
`vm` (the `dev/tests/_dom.js` + `_fixture.js` harness) and re-serialised through
`serializeRecords`, so every density figure below is of what the current build
would write rather than of what is on disk. No repository file was modified.
Two scratch files were written under `dev/tests/_scratch/` and deleted; that
directory is empty again, as it was before.

**One fact that conditions several findings.** The live corpus on disk was last
written by v3.14.241. It therefore does **not** carry D50 stage 4: the document
row has no `record_type`, the 52 dictionary sense rows have no `record_type`,
`prov` is an object on 134 objects and `prov_history` holds 75 inline objects,
the 33 lemma rows still carry `comments: []`, and the dictionary carries 22
inline `"Test Annotator #1"` display names. All of that is fixed in code and
migrates on load. Findings closed "in code" are closed; findings that describe
what a file on disk contains are still visible in that file until it is re-saved.

---

## Summary table

| id | subject | classification | fixed at |
|---|---|---|---|
| L-001 | A 300,000-word glossed corpus cannot be loaded or saved | **FIXED** (headline was wrong as filed; the real half is closed) | v3.14.253, B-116 |
| L-002 | D33 measured a bare corpus and reported it as annotated | **PARTLY FIXED** — correction recorded, re-measurement pass not done | DEV_PLAN corrected v3.14.229–231 |
| L-003 | The translation language is drawn, typed, stamped, and discarded | **STILL OPEN** (B-117) | — |
| L-004 | Editing a dictionary headword discards the change and stamps for it | **STILL OPEN** (B-118) | — |
| L-005 | The current lemma format is undeclared; two guards fail on real data | **FIXED** | v3.14.238 (B-119), extended v3.14.248, v3.14.249 |
| L-006 | Mutation score 56%, and one guard is entirely hollow | **PARTLY FIXED** — two guard defects fixed, the hollow guards unchanged | B-131/B-132 v3.14.254 |
| L-007 | The fixture is weaker than the guards it feeds | **PARTLY FIXED** — guards done, fixture data open | guards v3.14.245–250 |
| L-008 | Search folds object-language forms with `toLowerCase` | **STILL OPEN** (B-120) | — |
| L-009 | 82 of 83 dictionary links carry no provenance | **STILL OPEN**, re-measured 91 of 92 (B-121) | — |
| L-010 | Half of every annotated word is a record of who typed it | **SUPERSEDED** by its own re-measurement; no open item, stale numbers remain in its tail | shape changed v3.14.247–249 |
| L-011 | Auto-linking is reachable from exactly one place | **STILL OPEN** (B-123) | — |
| L-012 | Two functions write the same field under opposite rules | **STILL OPEN** (B-122) | — |
| L-013 | The observed annotation order corrects D46, and F5 with it | **PARTLY FIXED** — correction absorbed into D46, reorder unbuilt and unowned | doc correction v3.14.229 |
| L-014 | The per-keystroke scan is real, and B-053 names the wrong pool | **PARTLY FIXED** — misfiling corrected in DEV_PLAN, the scan unchanged; figures moved | doc correction v3.14.229 |
| L-015 | D33's trigger for writing the id-sort guard has been met | **FIXED** | v3.14.249 (D50 stage 4d) |
| L-016 | A word save silently deletes morpheme keys the table declares | **FIXED** — the audit does not say so | v3.14.236 (B-124) |
| L-017 | Fields declared fillable that no editor draws | **STILL OPEN**, all four cases | — |
| L-018 | The suite costs 17.9 s, and one guard is 80% of it | **PARTLY FIXED** — `--slow` shipped; the archive-gated guard unchanged | `--slow` v3.14.254 |
| L-019 | Two disabled guards gated on a filename | **STILL OPEN**, reproduced | — |
| L-020 | The legacy tier, per field | **FIXED**, with one deliberate carve-out | v3.14.249 (D50 stage 4d) |
| L-021 | The sample swap would silently delete dependency coverage | **PARTLY FIXED** — guard done, data open; its `samples/README.md` half is **WRONG AS FILED NOW** | guard v3.14.250 (B-125) |
| L-022 | Disposition of the twelve | **PARTLY FIXED** — 2 of 7 retired, F2's disposition still contested | retirements v3.14.230 |

Counts: 5 fixed, 8 partly fixed or superseded, 9 still open.

---

## Findings that are not fully fixed

### L-002 · PARTLY FIXED · D33's fixture density

**Evidence.** `dev/DEV_PLAN.md:228` carries the correction with a strikethrough
over D33's original conclusion, and `dev/DEV_PLAN.md:147` carries L-014's. So the
documentary half is done. What is not done is the pass the audit's §4.2 row 5
names: no synthetic corpus generator exists anywhere in the tree (`dev/tools/`
holds only `input_inventory.py` and its JSON; `dev/fixtures/` holds one HTML
preview), so nothing has been regenerated at real density.

**The multiplier in the finding is now wrong.** Measured today by loading the
live corpus through the app's loaders and re-serialising it (162 words, 37
annotated): an annotated word costs **923.6 characters**, a word stripped of all
annotation costs **101.6**, and the whole corpus is 65,903 characters for 162
words. D33's synthetic fixture was 235 B/word. The finding's "10.1× too small"
was computed against 2,381 B/word, a figure two format changes old; against the
current shape the fixture is about **3.9×** too small. The direction of the
finding survives; the number does not.

**To close:** regenerate the synthetic 50k and 300k corpora at the current
density and re-run D33's parse-cost and heap measurements. Code (a generator
plus a measurement script); no application change.

---

### L-003 · STILL OPEN · `document.translation_language`

**Evidence, code.** `source/modules/field_spec.js:78-80` declares the field with
`tier: 'core', control: 'language'` and no `planned` flag, so
`isFillable` (`field_spec.js:402-408`) returns true and the generated document
form draws it. `readForm` reads it (`source/LingCoT.html:6836-6858`, the `'language'` case at `:6844`). `formProvMap` (`source/LingCoT.html:6899-6906`) includes
every fillable scalar control and `'language'` is in `_PROV_SCALAR_CONTROLS`
(`:6898`), so `saveDocument`'s `stampFieldProv(d.metadata, formProvMap('document', v))`
at `source/LingCoT.html:8656` stamps it. Neither save handler assigns it:
`saveNewCorpus` (`:8534-8588`) builds `metadata` from a literal naming `title`,
`language`, `source_ids`, `comments`, the five bibliographic extras and
`metadata_prov`; `saveDocument` (`:8623-8672`) assigns `title`, `language`,
`source_ids`, `comments` and the five extras. Read at
`source/LingCoT.html:10696` (`translationTarget`) and in
`source/scripts/corpus_annotate.py`.

**Evidence, data.** The live corpus document's `metadata` keys are exactly
`title, language, source_ids, comments, metadata_prov, dict_key_version,
field_prov`. `metadata.field_prov` is `{ translation_language: 0 }`, and
event 0 in the file's `prov_events` table is
`{annotator_id: "ann_001", date: "2026-08-29", time: "13:50:58"}`. The stamp is
there; the value is not.

**To close:** one assignment in each of the two handlers, plus a guard of the
shape §4.2 row 4 specifies (`stampFieldProv(k)` with no assignment to `k`). Code.

---

### L-004 · STILL OPEN · `saveDictEntry` never assigns `entry.form`

**Evidence.** `source/LingCoT.html:9189-9260`. The handler assigns
`part_of_speech`, `type`, `gloss`, `meaning`, `constituent_forms`, `variants`,
`semantic_domain`, `usage_notes`, `lemma_id`, `transliterations`, `comments`,
`allomorphs` and `selection`, and never `form`. `form` is declared
`tier: 'identity', control: 'text'` at `source/modules/field_spec.js:205-206`, so
it is fillable, read by `readForm`, validated by `requireIdentity('dict_entry')`
at `:9198`, and stamped through `formProvMap('dict_entry', v)` at `:9245-9249`.
The comment at `:9253` states the assumption out loud: "form and id are never
edited here". `saveNewDictEntry` at `:9800` does write `form`
at `source/LingCoT.html:9816`.

**To close:** either assign `entry.form` and re-key `S.dictByForm`, or declare
the control `readonly` so the form stops offering an edit it discards. Code.

---

### L-006 · PARTLY FIXED · guard depth

**What is fixed.** Two real guard defects found during the v3.14.254
re-verification are fixed: B-131 (`suggest_mechanism_test`'s row fixture could
not catch B-082) and B-132 (`prov_clock_test`'s rollover check was
timezone-dependent). `dev/edit_log.md:10-49` records both, and
`dev/audits/GUARD_MUTATION_2026-08-30.md` holds the per-guard evidence.

**What is not fixed, and is reproducible today.** In a scratch copy of the tree
at v3.14.254, prepending `return null;` to each function in turn and running the
whole suite:

| gutted | suite result |
|---|---|
| `requireIdentity` | 63 passed, 0 failed, 4 disabled |
| `wantedMark` | 63 passed, 0 failed, 4 disabled |
| `makeWordObj` | 63 passed, 0 failed, 4 disabled |
| `confirmAnnotationLoss` | 63 passed, 0 failed, 4 disabled |
| `renderSectionAdd` | 63 passed, 0 failed, 4 disabled |
| `offerSrcOf` | 63 passed, 0 failed, 4 disabled |

63/0/4 is the clean baseline for that copy, so all six survive. Gutting
`takeOffer`, `resolveLemma` and `lemmaStripHtml` together leaves
`linking_s1s3_test.js` at **34 passed, 0 failed**, `autolink_test.js` at 19/0 and
`suggest_mechanism_test.js` at 76/0. `dev/tests/required_marker_test.js:106` is
still `check(/function wantedMark\(\)/.test(html), 'wantedMark() is defined')`.

**Re-measured assertion shape.** 1,006 `check(` sites across `dev/tests/*_test.js`
(the finding said ~1,009); 340 of them contain `.test(` against source text (the
finding said 313); 130 of those are negations.

**On the 56%.** Not reproducible and should not be re-quoted. §4.3 already says
so; the number is still printed in L-006's own headline.

**To close:** the three items §4.3 lists — give `linking_s1s3` a behavioural half
in a `vm` the way `morpheme_merge_test` already has one; rewrite
`suggest_mechanism`'s static half and `required_marker`'s sections 2 to 7 to
execute `renderField`; collapse the retired-spelling absence assertions into one
sweep per guard. Code (guards only).

---

### L-007 · PARTLY FIXED · the fixture

**Guards half, confirmed done.** Run against the live corpus with
`LINGCOT_TEST_CORPUS`: `field_spec_test` 63 passed / 0 failed (62 on
`samples/`), `lemma_registry_test` 46/0 (48 on `samples/`),
`schema_conformance_test` 2/0 walking **348 objects** on the live corpus against
**172** on the sample. `prov_intern_test` 50/0 on the live corpus. The whole
suite against the live corpus is 64 passed, 1 failed, 2 disabled, and the single
failure is `dep_root_test` failing on purpose (L-021).

**Fixture half, open, and it is data.** `samples/turkish-test/turkish-test_corpus.jsonl`
carries no `prov_events` line and no `record_type` on any row. Its dictionary is
32 rows: 22 sense entries (11 `word`, 11 `bound.morpheme`) and **10 rows still in
the pre-migration `type: 'lemma'` shape**, none carrying `record_type`. 27 rows
carry an `alternate_forms` key, all empty. 2 sections, 5 paragraphs, 13
sentences, 98 words of which 20 are punctuation, 20 morphemes. Against the live
corpus's 162 words, 12 sentences, 77 morphemes, 85 dictionary rows and an event
table of 60 moments.

**To close:** a replacement corpus saved by a current build, so it carries the
event table and `record_type`. Data, not code.

---

### L-008 · STILL OPEN · five notions of "same form"

**Evidence, code.** `source/modules/search_b.js` uses bare `.toLowerCase()` as
the frequency type key at `:692`, as the anchor sort key at `:717-718`, as the
context sort key at `:724` and as the collocate key at `:768` (the finding cites
:690 and :766; the lines drifted by two). `source/modules/search.js` uses the
`iu` regex flag at `:92`, `:146` and `:675`. `search_b.js:65-71` still states the
folding decision in a comment. `normForm` and `normMeta` are at
`source/LingCoT.html:1943` and `:1957`; `Intl.Collator(..., {sensitivity:'base'})` is
built at `source/LingCoT.html:2765-2768` and used only as a re-check.

**Evidence, data, re-run.** The live corpus holds exactly three `İkna` word
tokens. `/^ikna$/iu` matches none of them. `'İkna'.toLowerCase()` is
`"i̇kna"` (i + combining dot above), which is not `"ikna"`, so the
frequency and collocate keys fold it into a bucket a query cannot reach either.

**To close:** route `search_b.js`'s four keys and `search.js`'s matcher through
`normForm` / the fold context. Code.

---

### L-009 · STILL OPEN · unsigned dictionary links (re-measured)

**Evidence, code.** Only `autoLinkToken` stamps: `source/LingCoT.html:3204-3206`
sets `obj.dict_id` and then `obj.field_prov.dict_id = _derivedFieldProv()`.
`_pushTokenToDict` writes `dict_id` bare at four sites,
`source/LingCoT.html:3316`, `:3322`, `:3351` and `:3356`.
`ensureMorphemesFromParse` writes it bare at `source/LingCoT.html:1822` and
`:1862`, from `_dictFillForForm`'s return at `source/LingCoT.html:1522`, in the
same literal where it stamps gloss, transliterations and type as derived.

**Evidence, data, counted on the live corpus.** 16 word `dict_id` and 76
morpheme `dict_id`, 92 links. **One** carries a `field_prov.dict_id`, on the word
`Kuzey`, and it resolves to event 2,
`{annotator_id: null, annotator: "auto (lexicon)", derived: true}`. So 91 of 92
are unsigned, 98.9%, and **zero** links in either corpus were chosen by a person.
`dev/BUGS.md` B-121 already carries these numbers; the audit's L-009 headline
still says 82 of 83.

**To close:** §4.2 row 3's `linkDict(obj, entry, prov)` chokepoint, in the order
it states — unify the ambiguity rule, then stamp, then backfill through it. Code.

---

### L-010 · SUPERSEDED · provenance share

**Re-measured today**, on what v3.14.254 would write for the live corpus. Over
the 37 annotated words, `field_prov` + `prov` + `prov_history` are **9,173 of
34,175 characters, 26.8%**; the same 37 words with every moment expanded inline
(`S.docs.map(expandProv)`) are 41,847 of 66,849, **62.6%**. The file's shared
event table is 6,401 characters. Across both files, stripping all four
provenance keys takes 85,386 characters to 61,888, so provenance is **27.5%** of
what reaches disk, not 51.6%.

**Nothing is open here** — the shape it named was changed by D50 stage 4. What
should be corrected is the finding's own closing paragraph, which still reads
"Removing it saves 51.6% of the file" and "300,000 words falls from 681 MB to
329.60 MB". Both are pre-interning figures sitting below a paragraph that says
they are.

---

### L-011 · STILL OPEN · auto-linking reachable from one place

**Evidence, code.** `autoLinkWord` is defined at `source/LingCoT.html:3213` and
called exactly once, from `_finishSave` inside `saveWord` at
`source/LingCoT.html:9161`.

**Evidence, data, recomputed through the app's own `lookupDict`.**

| | words | touched | touched & linked | touched, matchable, unlinked | untouched & linked | untouched, matchable, unlinked |
|---|---|---|---|---|---|---|
| live | 162 | **37** | **16** | **0** | 0 | **16** |
| sample | 98 | **17** | 12 | 0 | 0 | 19 |

The 16 live misses are `ve` ×5, `Bu` ×3, `ki` ×3, `Kuzey` ×2, `Güneş`, `yol`,
`bir`, each with exactly one candidate. The live dictionary has **0** forms that
fold to the same key under `normForm`, so a backfill is unambiguous today. The
audit's own table still says 33 touched and 13 for the sample; both are stale
(37 and 17). `dev/BUGS.md` B-123 has the current numbers.

**To close:** a one-shot backfill over untouched tokens, run *through* the
chokepoint of §4.2 row 3 rather than before it. Code.

---

### L-012 · STILL OPEN · two writers, opposite ambiguity rules

**Evidence.** `autoLinkToken` refuses an ambiguous form and calls
`linkNote('note.link.ambiguous', ...)` at `source/LingCoT.html:3200-3202`.
`_dictFillForForm` at `source/LingCoT.html:1519-1522` takes
`(cands.find(e => e.type === 'word' || e.type === 'bound.morpheme') || cands[0])`
with no ambiguity check at all. Counted on the live corpus: **76 of the 77
morpheme links** come from that path; the one unlinked morpheme has zero
candidates, not two. Zero ambiguous forms in the live dictionary today, so the
rule has not yet cost anything. The sample dictionary now has **one** form that
folds to a shared key, which the live one does not.

**To close:** same chokepoint as L-009. Code.

---

### L-013 · PARTLY FIXED · the annotation order

**The correction landed as documentation.** `dev/DEV_PLAN.md:787-825` carries the
observed order, the correction to F5 and the target order verbatim, and
`dev/DEV_PLAN.md:163` strikes F5 "retired on evidence".

**The reorder is unbuilt and still unowned.** `renderWordEdit`
(`source/LingCoT.html:7856`) draws, in DOM order: transliteration `:7950`, gloss
`:7955`, POS `:7964`, lemma `:7969`, parse `:7978`, morpheme rows `:7984`.
`FIELD_SPEC.word` (`source/modules/field_spec.js:164-186`) declares
`form, morphological_parse, morphemes, part_of_speech, gloss, lemma_id,
transliterations, comments`. The observed order is translit, POS, parse,
morphemes, lemma, gloss. Three orders, none matching, which is conflict ⑥ intact.
`formBodyHtml` is called for `paragraph`, `sentence`, `dict_entry`, `document`
and `section` (`source/LingCoT.html:6968, 7382, 8022, 8096, 8116, 8132, 8172,
8214, 8251, 8269`) and for neither `word` nor `morpheme`.

**To close:** convert `renderWordEdit` to `formBodyHtml` and reorder
`FIELD_SPEC.word` — §4.2 row 2, which also closes ⑦, L-017's `word.comments` and
part of L-016's family. Code (markup), and it needs a plan owner.

---

### L-014 · PARTLY FIXED · the per-keystroke scan

**The misfiling half is corrected.** `dev/DEV_PLAN.md:147` records B-053 as filed
against the wrong pool, in L-014's words.

**The scan is unchanged.** `tagUsage` at `source/LingCoT.html:3802-3821` walks
every word, every morpheme and the whole of `S.dictionary`, memoised on
`_dataGen` and invalidated on the first line of the function.
`AC_POOLS.pos` (`source/LingCoT.html:3586`) and `AC_POOLS.type` (`:3590`) call it
per element; `AC_POOLS.domain` (`:3607`) does not.

**Re-measured.** A synthetic corpus built by replicating the live document's
sentences at live density, loaded through `applyCorpus`, with `_dataGen` bumped
before each timing:

| corpus | `tagUsage` both fields | `AC_POOLS.pos` | `AC_POOLS.type` | `AC_POOLS.domain` |
|---|---|---|---|---|
| 50,220 words | 10–21 ms | 11 ms | 10 ms | **0 ms** |
| 300,186 words | 66–90 ms | 66 ms | 73 ms | **0 ms** |

The finding's 22 ms at 50k is reproduced; its **131 ms at 300k is not** — the
current figure is 66–90 ms. `domain` is confirmed the cheapest of the three, at
the resolution of `Date.now()`.

**To close:** an incremental counter maintained on write, or a memo keyed to
something finer than `_dataGen`. Code. It is not urgent at the sizes measured.

---

### L-017 · STILL OPEN · fields declared fillable that nothing draws

**Evidence, all four still true.**

- `word.comments` — `source/modules/field_spec.js:178`, `tier: 'aux',
  control: 'comments'`, fillable. `renderWordEdit` (`source/LingCoT.html:7856`)
  draws six controls and none of them is a comments editor; there is no element
  with id `f-word-comments` anywhere in the tree.
- `morpheme.comments` — `source/modules/field_spec.js:200`. `morphEditRowsHtml`
  (`source/LingCoT.html:7413`) draws gloss `:7451`, POS `:7460` and type `:7469`
  only.
- `morpheme.transliterations` — `source/modules/field_spec.js:199`, declared
  `blocked: 'D32'`. `isFillable` (`source/modules/field_spec.js:402-408`) tests
  `tier`, `surface` and `planned` and **does not consult `blocked`**, so the
  field counts as fillable while nothing draws it.
- `lemma.form` — `source/modules/field_spec.js:271`, `tier: 'identity',
  control: 'text'`. The only surface that touches it is
  `quickLemmaPanelHtml` (`source/LingCoT.html:6769-6799`), which **creates**
  lemmas; there is no lemma edit view, so a mis-typed citation form still cannot
  be corrected in the app.
- The partial case also holds: `_deriveWordFields`
  (`source/LingCoT.html:1768-1777`) writes `word.transliterations` when
  `ex.translit` is truthy, and its `else if` branch is
  `!(Array.isArray(word.transliterations) && word.transliterations.length)`, so
  clearing the box on a word that already has a transliteration changes nothing.

**To close:** the first two and the last arrive free from §4.2 row 2 (convert
`renderWordEdit`). `morpheme.transliterations` needs D32 decided, or
`isFillable` taught to read `blocked`. `lemma.form` needs an edit surface, which
is D35 B5's group view. Code.

---

### L-018 · PARTLY FIXED · suite cost

**The `--slow` half shipped**, v3.14.254. Measured here: default run
**5.6 s**, 65 passed / 0 failed / 2 disabled; `--slow` **20.3 s**, 66 passed / 0
failed / 2 disabled. `nllb_diag_test.py` alone is **14.4 s** for 34 assertions,
71% of the slow run.

**The `undefined_call_test` half is unchanged.**
`dev/tests/undefined_call_test.js:206-216` still prints DISABLED and
`process.exit(fail ? 1 : 2)` when `dev/archive/changes/b062_log_undefined/` is
absent, after its real check has already passed. Reproduced by copying the tree
without `dev/archive/`: the clone reports **63 passed, 0 failed, 4 disabled**,
the extra two being `undefined_call_test.js` and `doc_integrity_test.js`. The
finding said a clone reads one more disabled guard than it has; it reads two.
This is conflict ⑪ from the other side.

**Re-measured ratios.** 68 executables (66 `*_test.js`, `log_triage.js`,
`nllb_diag_test.py`), **14,282** lines of guard including the four `_*.js`
helpers, against **24,839** lines of application (`LingCoT.html`, the seven
modules, `LingCoT.pyw` and the Python under `source/`), **0.58 to 1**, not
0.49 to 1.

**To close the remaining half:** make the self-verification's fixture portable
(a small checked-in pre-fix snippet rather than an archive file), so the guard's
real check is never discarded. Code. Must be sequenced with conflict ⑪'s
archive-freeze commit.

---

### L-019 · STILL OPEN · the two disabled guards

**Evidence, reproduced.** `dev/tests/search_b_matcher_test.js:27` and
`dev/tests/search_b_concordance_test.js:17` both call
`requireCorpusOrDisable('korean', ...)` from `dev/tests/_fixture.js:104-116`,
which matches on the substring of the file path. Copying the live Turkish corpus
to `korean-test/korean-test_corpus.jsonl` and pointing `LINGCOT_TEST_CORPUS` at
it makes both run immediately: **matcher 5 passed / 10 failed**, **concordance
4 passed / 13 failed**, exactly the split the finding reports. The gate is sound
and the disable is honest.

**The coverage cost, confirmed.** `sbBuildKwic`, `sbBuildFrequency`,
`sbSortKwic` and `sbBuildCollocates` (`source/modules/search_b.js:607`, `:669`,
`:707`, `:735`) are named by **no** guard except
`dev/tests/search_b_concordance_test.js`, which is off. Grepping all of
`dev/tests/` for those four identifiers returns that one file. The whole KWIC,
frequency and collocates feature has zero coverage while the gate holds.

**To close:** split each guard into a corpus-independent half (empty query,
validation, routing, error paths, sort order) that always runs and a
golden-string half that stays gated, or supply the Korean fixture. Code for the
split; data for the fixture.

---

### L-021 · PARTLY FIXED · the fixture swap

**Guard half done and verified.** Against `samples/`, `dep_root_test.js` reports
13 `head`, 10 `dep_rel`, 3 parsed sentences and passes with 16 assertions.
Against the live corpus it reports 0, 0, 0 and **fails**, 14 passed / 2 failed,
naming what the replacement corpus needs. Confirmed by running the whole suite
under `LINGCOT_TEST_CORPUS`: 64 passed, 1 failed, 2 disabled, and that one
failure is this guard.

**Data half open.** The live corpus has zero `head` keys and zero `dep_rel`
across 162 words. Hand-annotated dependency parses are the prerequisite and no
amount of code substitutes for them.

**The README half of this finding is no longer true.** L-021 says
"`samples/README.md` is still stale. Counted at v3.14.250 it claims 3 sentences /
17 words / 18 dictionary entries / 54 schema-checked objects ... It also still
says six guards read a corpus." The file was refreshed in that same version:
`dev/edit_log.md:235-238` records the sweep, and `samples/README.md` now states
13 sentences, 98 words of which 20 are punctuation, 20 morphemes, 22 entries (11
`word`, 11 `bound.morpheme`) plus 10 lemma records, 172 schema-checked objects,
3 parsed sentences with 13 heads and 10 relations, and thirteen guards. Every one
of those numbers was re-counted here and every one is correct.

**To close:** annotate dependency parses in the replacement corpus, then swap.
Data.

---

### L-022 · PARTLY FIXED · disposition of the twelve

**Executed.** Two of the seven named for supersession were retired at v3.14.230:
`retired/LINKING_AUDIT.md` and `retired/LEMMA_LAYER_AUDIT.md`
(`dev/audits/AUDIT_INDEX.md:59-60`). D33's supersession is recorded at
`dev/DEV_PLAN.md:228`.

**Not executed.** `DATA_MODEL_AUDIT.md` (SPENT, §1 wrong, F1/R28 open),
`ANNOTATION_FILL_AUDIT.md` (WRONG and partly live, B-058 and F2/F3/F5 open),
`SCRIPTS_AUDIT.md` (SPENT, S5 open) and `INPUT_UX_AUDIT.md` (LIVE, five items)
all still sit in `dev/audits/` with open items against them, per
`dev/audits/AUDIT_INDEX.md:52-58`. The disposition was a recommendation and the
index did not adopt it.

**F2 to F5.** F4's mechanism is recorded as shipped and F5 as retired
(`dev/DEV_PLAN.md:250-262`). F2's disposition is still contradicted inside the
same file: `dev/DEV_PLAN.md:253` says close it into D40, `:151` says it is
half-built and unblocked at gate 3. That is conflict ⑭, unresolved. F3 is still
"not built" (`dev/DEV_PLAN.md:265`) despite the finding raising its priority.

**To close:** settle F2 in one place, and either retire the four documents or
strike the recommendation. Documentation.

---

## What contradicts what the audit currently says

1. **L-016 is fixed and the audit does not say so.** `saveWord` no longer
   rebuilds `word.morphemes` from a bare literal: `source/LingCoT.html:9083`
   calls `mergeMorpheme(existing, {...})`, and `mergeMorpheme`
   (`source/LingCoT.html:2178-2185`) copies every key of `existing` and applies
   only the panel's own keys, with `undefined` meaning delete. Fixed as B-124 at
   v3.14.236 (`dev/edit_log.md:786-787`). §4.2 row 4 records B-124 as done but
   does not connect it to L-016, and L-016 carries no closure marker.

2. **L-021's `samples/README.md` paragraph is wrong.** The file was refreshed in
   the same version the paragraph cites, v3.14.250. Detail under L-021 above.

3. **L-009's and L-011's headline numbers are stale in the audit and current in
   `dev/BUGS.md`.** The audit says 82 of 83 links and a 33/16 touched split;
   measured today it is 91 of 92 and a 37/16 split, which is what B-121 and B-123
   already say. Two documents, two numbers, and the audit is the one people read
   first.

4. **L-010's closing paragraph contradicts L-010's own re-measurement.** The
   re-measurement paragraph gives 26.8%; four lines later the finding still says
   removing provenance "saves 51.6% of the file" and takes 300,000 words "from
   681 MB to 329.60 MB". Measured on the current shape the saving is 27.5% across
   both files.

5. **L-014's 131 ms at 300,000 words does not reproduce.** Measured 66–90 ms on
   a synthetic corpus at live density. The 22 ms at 50,000 words does reproduce.
   `dev/DEV_PLAN.md:147` quotes both figures.

6. **L-006's "313 of ~1,009 assertion sites" is now 340 of 1,006**, and its 56%
   mutation score is unreproducible — §4.3 says so, but the headline still
   carries the number.

7. **L-018's 0.49-to-1 guard-to-application ratio is now 0.58 to 1**
   (14,282 against 24,839 lines), and a clone loses two guards to the missing
   archive, not one.

8. **L-020's "19 sample `alternate_forms`" is 27.** Counted in
   `samples/turkish-test/turkish-test_dictionary.jsonl`: 27 of 32 rows carry the
   key, all of them empty arrays. The same figure is repeated in the code comment
   at `source/LingCoT.html:1640-1645`. The finding's substantive point — that
   they are all empty, so nothing was at risk — is unaffected.

9. **A field written by two writers and read by nothing, named in no finding.**
   `word_index` is written by `makeWordObj` at `source/LingCoT.html:8287` and by
   `source/scripts/corpus_ingest.py:1061`, and declared `tier: 'derived'` at
   `source/modules/field_spec.js:182`. Grepping all of `source/`, `docs/` and
   `dev/tests/` finds no reader. The comment beside the write at
   `source/LingCoT.html:8286` says "used by search", and neither `search.js` nor
   `search_b.js` mentions it. This is the same shape as L-020's `definition`
   finding — a comment promising a reader that does not exist — and it is on
   every word of every corpus.

10. **The live corpus on disk is two format generations behind the code, and
    three "closed" findings are still visible in it.** The file was last written
    by v3.14.241. Its document row has no `record_type`; its 52 dictionary sense
    rows have no `record_type` (L-005's stage 4c); its 33 lemma rows still carry
    `comments: []` (L-020); `prov` is an inline object on 134 objects and
    `prov_history` holds 75 inline objects (D50 stage 4b); and the dictionary
    carries **22 inline `"Test Annotator #1"` display names**, so the corpus is
    not PI-free on disk. §5.4 says B-129 was "re-measured end to end on the live
    corpus: 19 names → 0". That is true of what the current build would write and
    false of the bytes now in `~/mnt/turkish_test_corpus`. Anything that leans on
    "the corpus is PI-free" — conflict ④, `file_layout_options.md` §2A — should
    say "as re-saved by v3.14.246 or later".

11. **`dev/BUGS.md` B-117 cites `LingCoT.html:10626` for `translationTarget`; it
    is at `:10696`.** Line drift from v3.14.253. Minor, but the audit's practice
    is to cite lines.

12. **Nothing marked closed was found still reproducible.** L-001, L-005, L-015
    and L-020 were each checked against the code and, where they had one, against
    a falsification: `id_sort_test.js` fails when an `.id` comparator is injected
    (10 passed, 1 failed); `field_spec_test`, `lemma_registry_test` and
    `schema_conformance_test` all pass against the live corpus as well as the
    sample; `LingCoT.pyw:406-417` measures each project file before reading it
    and `source/modules/events.js:722-731` wraps `serializeRecords` in a
    try/catch on the write path. The closure markers in §1 are accurate. The
    inaccuracies are all in numbers, not in verdicts, with the two exceptions
    above (L-016 unmarked, L-021's README paragraph).
